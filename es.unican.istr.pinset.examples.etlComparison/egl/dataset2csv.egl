[%
  var sep = ",";
  var columns = dataset.columns;
  var columnNames = columns.collect(c | c.name);
  out.println(columnNames.concat(sep));
  for (row in dataset.rows) {
    out.println(row.cells.collect(c | c.value).concat(sep));
  }
%]
